from covid_model_deaths.deaths_io import InputsContext, Checkpoint, MEASURES


def run_deaths_model(app_metadata, input_root, output_root):
    inputs = InputsContext(input_root)
    checkpoint = Checkpoint(output_root)





    pass
